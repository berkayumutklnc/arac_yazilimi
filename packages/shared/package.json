{
  "name": "@arac-yazilim/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:related": "vitest related --run",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "generate:diag-types": "openapi-typescript openapi/diag-service.json -o src/generated/diagServiceTypes.ts"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0"
  }
}
